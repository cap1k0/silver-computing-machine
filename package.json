{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["apps/*"],
  "devDependencies": {
    "nx": "20.3.0",
    "@nx/next": "20.3.0",
    "@nx/workspace": "20.3.0",
    "typescript": "^5"
  },
  "scripts": {
    "frontend:dev": "nx dev frontend",
    "frontend:build": "nx build frontend",
    "shell:dev": "nx dev shell",
    "shell:build": "nx build shell",
    "build:all": "nx run-many --target=build --all"
  }
}
